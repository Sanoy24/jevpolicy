---
'@sanoy24/jevpolicy': minor
---

Add compatible dual-policy shadow evaluation with one shared provider request,
separate live and shadow envelopes, comparison metadata, recording support, and
the `evaluate --shadow-policy` CLI option.
